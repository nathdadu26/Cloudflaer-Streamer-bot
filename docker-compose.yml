version: '3.8'

services:
  bot:
    build: .
    container_name: cloudflare-streamer-bot
    restart: always
    env_file: .env
    volumes:
      - ./downloads:/app/downloads
    environment:
      - PYTHONUNBUFFERED=1
